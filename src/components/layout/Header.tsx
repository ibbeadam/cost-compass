// This file is no longer used as the header is now part of the global layout structure.
// Keeping it empty to avoid build errors if it's imported somewhere,
// but it should ideally be deleted if no longer referenced.
// The new header is implemented in src/components/layout/AppHeader.tsx
// and integrated into src/app/layout.tsx.

export {};
